zip :: ([A], [B]) => [A B]
lambda ([] _) []
lambda (_ []) []
lambda ((x:xs) (y:ys)) (Cons (x y) (zip xs ys))

action main
    let lista_a [1 2 3]
    let lista_b [10 20 30]
    let z (zip lista_a lista_b)
    assert! (= z [(1 10) (2 20) (3 30)])
    echo! "Zip Concluido"

main!